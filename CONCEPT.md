# コンセプト

* 「大喜利」のWebサービス
* アカウントを持つユーザーは全ての構成要素を投稿できる
* ユーザーごとの一覧表示も可能
* 構成要素は下記
    * お題
    * 回答
    * 回答の評価（0 〜 10点）
    * 回答へのコメント

## 利用するライブラリ等

* Node.js
* Hono
* Prisma
* React 

## 今回の制約

* ユーザー認証はプロバイダでの切り替えが出来ることを目指すが、今回は「ユーザー申告のID＋PW」をそのまま受け入れる方向で実装する。
* HonoはAPIサーバーとして利用し、Reactはクライアントとして利用する。
    * そのため、APIサーバーはRESTful APIを実装する
    * クライアントはRESTful APIを呼び出して表示する
* ER図を残す
* UIはシンプル＋ポップ
* 最終的なデプロイ先はCloudflare
* 開発にはDocker＋Docker Composeを利用する